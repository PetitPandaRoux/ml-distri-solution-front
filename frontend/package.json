{
  "name": "ml-distri-solution-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "NUXT_APP_BASE_URL=/ml-distri-solution-front/ npm run generate && gh-pages --dotfiles -d .output/public",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^3.12.0",
    "better-sqlite3": "^12.6.2",
    "nuxt": "^4.4.2",
    "pocketbase": "^0.26.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.2.1"
  }
}
